<nav class="admin-navbar navbar navbar-expand-lg">
    <div class="container">

        <!-- Logo -->
        <a asp-controller="Admin" asp-action="Index" class="navbar-brand admin-brand">
            Admin Panel
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContext">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between" id="navbarContext">

            <!-- Sol Menü -->
            <ul class="navbar-nav admin-nav gap-3">
                <li class="nav-item">
                    <a asp-controller="Kategori" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Kategori" ? "active" : "")">
                        Kategori
                    </a>
                </li>

                <li class="nav-item">
                    <a asp-controller="Urun" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Urun" ? "active" : "")">
                        Ürün
                    </a>
                </li>

                <li class="nav-item">
                    <a asp-controller="Slider" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Slider" ? "active" : "")">
                        Slider
                    </a>
                </li>

                <li class="nav-item">
                    <a asp-controller="Role" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Role" ? "active" : "")">
                        Rol
                    </a>
                </li>

                <li class="nav-item">
                    <a asp-controller="User" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "User" ? "active" : "")">
                        Kullanıcı
                    </a>
                </li>

                <li class="nav-item">
                    <a asp-controller="Order" asp-action="Index"
                       class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Order" ? "active" : "")">
                        Siparişler
                    </a>
                </li>
            </ul>

            <!-- Sağ Menü -->
            <div class="d-flex align-items-center gap-3">

                <span class="nav-user">
                    <i class="fa-regular fa-user me-1"></i>
                    @User.Identity?.Name
                </span>

                <a asp-controller="Account" asp-action="Logout" class="logout-icon">
                    <i class="fa-solid fa-right-from-bracket"></i>
                </a>

            </div>

        </div>
    </div>
</nav>
