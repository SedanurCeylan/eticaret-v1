@model e_ticaret_proje.Models.AdminDashboardViewModel

<div class="row mt-3">
    <div class="col">
        <div class="card shadow-sm dashboard-card">
            <div class="card-header dashboard-card-header">
                <h5 class="card-title mb-0">Yeni Siparişler</h5>
            </div>

            <div class="card-body">
                <div class="table-responsive">
                    <table class="table dashboard-table mb-0 align-middle">
                        <thead>
                            <tr>
                                <th>Sipariş Id</th>
                                <th>Kullanıcı Adı</th>
                                <th>Toplam</th>
                                <th>Tarih</th>
                                <th>Durum</th>
                                <th style="width: 40px;"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.LastOrders != null && Model.LastOrders.Any())
                            {
                                @foreach (var item in Model.LastOrders)
                                {
                                    <tr>
                                        <td>@item.OrderId</td>
                                        <td>@item.Username</td>
                                        <td>@item.ToplamFiyat.ToString("C")</td>
                                        <td>@item.SiparisTarihi.ToShortDateString()</td>
                                        <td>
                                            <span class="order-status">Onay Bekliyor</span>
                                        </td>
                                        <td>
                                            <a asp-controller="Order"
                                               asp-action="Details"
                                               asp-route-id="@item.OrderId"
                                               class="btn btn-icon">
                                                <i class="fa-solid fa-right-long"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="6" class="text-center text-muted">
                                        Henüz yeni sipariş bulunmuyor.
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
